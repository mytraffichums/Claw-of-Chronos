# ── Relay Server Environment Variables ────────────────────────────────

# ChronosCore contract address (deployed on Monad)
# Required: relay will exit with error if not set
CHRONOS_CORE=0xc3F988DfFa5b3e49Bb887F8eF86c9081Fa381e97

# Monad RPC endpoint
# Public RPC: https://rpc.monad.xyz
# Local testnet: http://localhost:8545
RPC_URL=https://rpc.monad.xyz

# Relay server port
# Default: 3001
RELAY_PORT=3001

# Optional: Chain polling interval (milliseconds)
# Default: 2000 (2 seconds)
# POLL_INTERVAL=2000
